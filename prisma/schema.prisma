datasource db {
  provider = "sqlite"
}

generator client {
  provider = "prisma-client-js"
}

model TOTO {
  id        Int     @id @default(autoincrement())
  title     String
  body      String
  createdAt DateTime @default(now())  
}
